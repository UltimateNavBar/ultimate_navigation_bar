name: Flutter tests

on:
  pull_request:
    branches:
      - dev

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: subosito/flutter-action@v1
        with:
          flutter-version: 3.13.9

      - name: Run tests
        run: flutter test
